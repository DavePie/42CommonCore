#include "../libft.h"

t_list  *ft_lstnew(void *content);

t_list  *ft_lstmap(t_list *lst, void *(*f)(void *),
    void (*del)(void *))
{
    t_list  *nlst;

    nlst = ()
    while(lst)
    {
        ans = (t_list *)malloc(sizeof(*ans));
        if (ans)
        {
            ans->content = content;
            ans->next = 0;
        }
    }
}