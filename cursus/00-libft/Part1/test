
#include "ft_memset.c"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int	main(int argc, char **argv)
{
	// char *a;
	// if (argc > 2)
	// {
		
	// 	a = malloc(sizeof(char) * atoi(argv[2]));
	// 	if (!a)
	// 	{
	// 		printf("memory error\n");
	// 		return (0);
	// 	}
	// 	ft_memset(a, argv[1][0], atoi(argv[2]));
	// 	for (int i = 0; i < atoi(argv[2]) + 2; i++)
	// 		printf("%c, ", a[i]);
	// 	printf("\n");
	// }
	// free(a);

	printf("%s\n", strnstr("hello there", "there", 11));
	return 0;
}